import * as React from "react";
let styles = require("./HeaderText.css");
export function HeaderText(props) {
    return <span className={styles['header']}>{props.children}</span>;
}
//# sourceMappingURL=HeaderText.jsx.map